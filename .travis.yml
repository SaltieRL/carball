language: python
python:
- "3.6"
- "3.7"
# command to install dependencies
install:
- pip install -r requirements.txt
- sudo apt-get update
- sudo apt-get install libprotobuf9=2.6.1
- sudo apt-get install libprotobuf-c0
# command to run tests
script:
- python init.py
- pytest
